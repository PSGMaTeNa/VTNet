[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
# For Json-serialization and deserialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

#For Room and msg Ids
uuid = {version = "1.0", features = ["v4", "serde"]}

#For Signatur/Nonce with JSON
base64 = "0.22"